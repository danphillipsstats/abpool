% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.abpool.R
\name{confint.abpool}
\alias{confint.abpool}
\title{Credible intervals for approximate Bayesian pooling}
\usage{
\method{confint}{abpool}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{abpool}, returned by \code{\link[=abpool]{abpool()}}.}

\item{parm}{A character or numeric vector specifying the parameters for which credible intervals should be calculated. If \code{NULL}, intervals are calculated for all parameters.}

\item{level}{Credible interval level. A single numeric value between 0 and 1. Defaults to \code{0.95}.}

\item{...}{Additional arguments. Currently unused.}
}
\value{
A matrix containing the lower and upper credible limits for each requested parameter, with one row per parameter.
}
\description{
Calculate credible intervals using posterior samples from approximate Bayesian pooling (ABpool).
}
\details{
Credible intervals are calculated using empirical quantiles of the posterior samples returned by \code{\link[=abpool]{abpool()}}.
}
\examples{

# Generate data
set.seed(1)
n <- 400; nobs <- 40
X <- rnorm(n); Z <- sqrt(0.9)*rnorm(n) + sqrt(0.1)*X
beta_0 <- 0; beta_X <- 0.8; beta_Z <- 0.2
eta <- beta_0 + X*beta_X + Z*beta_Z
Y <- rbinom(n, size = 1, p = exp(eta)/(1 + exp(eta)) )
X[1:(n-nobs)] <- NA
log.data <- data.frame(Y = Y, X = X, Z = Z)
m <- 200
# Impute using mice (200 imputations, predictive mean matching)
imp <- mice::mice(log.data, m = m, method = "pmm", print = FALSE)
fits <- with(
  imp,
  glm(Y ~ X + Z, family = binomial)
)
# Fit ABpool
abpool.out <- abpool(fits)
confint(abpool.out)

}
\seealso{
\code{\link[=abpool]{abpool()}}
}
