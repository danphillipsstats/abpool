% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abpool.R
\name{abpool}
\alias{abpool}
\title{Approximate Bayesian pooling}
\usage{
abpool(object, parameters = NULL, dfcom = NULL, J = 1)
}
\arguments{
\item{object}{A \code{mira} object, or a list of fitted model objects, where the \code{l}th element gives the model fit to the \code{l}th imputed dataset.}

\item{parameters}{A character vector of parameter names, or a numeric vector of parameter positions to include in ABpool. If \code{parameters = NULL}, ABpool will be performed for all parameters in the model. The parameters are included in the order specified.}

\item{dfcom}{The complete-data degrees of freedom used for the t-distributed complete-data posterior approximation. \code{dfcom = Inf} corresponds to a Gaussian approximation. If \code{NULL}, \code{abpool()} attempts to extract the degrees of freedom from each fitted model using \code{df.residual()}; an error is returned if extraction fails or the extracted values differ across imputations. We recommend specifying \code{dfcom} when the appropriate complete-data degrees of freedom are known.}

\item{J}{Number of samples drawn per imputed dataset. Defaults to \code{1}.}
}
\value{
An object of class \code{abpool}. The object is a list containing
\describe{
\item{samples}{ABpool posterior samples. A numeric vector of length \code{m * J} for a single parameter, or an \code{m * J} by \code{p} matrix for \code{p} parameters.}
\item{estimates}{Parameter estimates extracted from the fitted models. A numeric vector of length \code{m} for a single parameter, or a list of \code{m} numeric vectors of length \code{p} for multiple parameters.}
\item{variances}{Variance estimates extracted from the fitted models. A numeric vector of length \code{m} for a single parameter, or a list of \code{m} variance-covariance matrices of dimension \code{p} by \code{p} for multiple parameters.}
\item{dfcom}{The complete-data degrees of freedom used for sampling.}
\item{parameters}{A character vector containing the names of the parameters included in ABpool, in the order used for sampling.}
\item{J}{The number of samples drawn per imputed dataset.}
\item{m}{The number of imputed datasets.}
\item{imputation}{An integer vector of length \code{m * J} identifying the imputed dataset associated with each element (scalar case) or row (multivariate case) of \code{samples}.}
}
}
\description{
Perform approximate Bayesian pooling (ABpool).
}
\details{
Given model fits from multiply imputed datasets, \code{abpool()} generates posterior samples using approximate Bayesian pooling (ABpool).
The function first extracts estimates and variances for the parameters from each fitted model. It then generates ABpool posterior samples from a t-distribution with location given by the estimates, and squared scale (or scale matrix) given by the variances. For \code{dfcom = Inf}, draws are from the corresponding Gaussian distribution.

The \code{abpool} function samples from an approximation to the observed-data posterior distribution, assuming the completed-data posterior distribution given each imputed dataset is t-distributed, with degrees of freedom equal to \code{dfcom}, location given by the estimate, and squared scale (or scale matrix) given by the variance estimate.
The input \code{object} may be:
\enumerate{
\item A list of model fit objects, with each elemnent generated from a function such as \code{lm()}, \code{glm()}, \code{survival::coxph()}.
\item An object of class \code{mira} generated by applying \code{with()} to a \code{mids} object from the \code{mice} package.
Suitable \code{coef()} and \code{vcov()} methods must be available for the fitted models. Classes supported include outputs from \code{lm()}, \code{glm()} and \code{survival::coxph()} (where \code{dfcom} must be user-supplied for \code{coxph()} models).
}

Inference using ABpool generally requires substantially more imputations than Rubin's rules. We recommend using at least 200 imputations when comparing Rubin's rules with ABpool using a Q--Q plot (\code{QQ_compare_abpool_rubin()}). Even larger numbers of imputations may be required for final inference using ABpool to achieve appropriate coverage (Phillips, Christodoulou and Steinsaltz, 2026).
}
\examples{

# Generate data
set.seed(1)
n <- 400; nobs <- 40
X <- rnorm(n); Z <- sqrt(0.9)*rnorm(n) + sqrt(0.1)*X
beta_0 <- 0; beta_X <- 0.8; beta_Z <- 0.2
eta <- beta_0 + X*beta_X + Z*beta_Z
Y <- rbinom(n, size = 1, p = exp(eta)/(1 + exp(eta)) )
X[1:(n-nobs)] <- NA
log.data <- data.frame(Y = Y, X = X, Z = Z)
m <- 200
# Impute using mice (200 imputations, predictive mean matching)
imp <- mice::mice(log.data, m = m, method = "pmm", print = FALSE)
fits <- with(
  imp,
  glm(Y ~ X + Z, family = binomial)
)
# Fit ABpool
abpool(fits)

}
\references{
Phillips, Christodoulou and Steinsaltz (XXXX)
}
\seealso{
\code{\link[=qqplot_rubin_abpool]{qqplot_rubin_abpool()}} to compare Rubin's rules and ABpool in a quantile-quantile (Q--Q) plot, \code{\link[=abpool_sample]{abpool_sample()}} to sample from a vector/list of estimates and variances, \code{\link[mice:with.mids]{mice::with.mids()}} to generate a \code{mira} object containing a list of model fits, \code{\link[mice:mice]{mice::mice()}} to impute missing data, the output of which can be used as an input for \code{\link[=with]{with()}} to generate the \code{mira} object.
}
