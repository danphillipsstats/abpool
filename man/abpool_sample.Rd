% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abpool_sample.R
\name{abpool_sample}
\alias{abpool_sample}
\title{abpool_sample}
\usage{
abpool_sample(estimates, variances, dfcom, J = 1)
}
\arguments{
\item{estimates}{Estimates from the imputed datasets.}

\item{variances}{Associated variances or variance-covariance matrices for the estimates from the imputed datasets.}

\item{dfcom}{Complete-data degrees of freedom.}

\item{J}{Number of samples per imputation.}
}
\value{
A numeric vector of posterior draws for a scalar parameter, or a
matrix of posterior draws for multiple parameters.
}
\description{
Draw samples via approximate Bayesian pooling (ABpool).
}
\details{
For each imputed dataset, sample from a t-distribution with location given by \code{estimates}, and scale given by \code{variances}. For \code{dfcom = Inf}, draws are from the corresponding Gaussian distribution.
}
\examples{
# Scalar input
estimates <- c(1,2,3)
variances <- c(1,2,3)
abpool_sample(estimates, variances, dfcom = Inf)

# Multi-parameter input
estimates <- list(
c(x = 1, z = 2),
c(x = 3, z = 4)
)
variances <- list(
  matrix(c(1,0.1,0.1,2), nrow = 2, ncol=2),
  matrix(c(1,0,0,1), nrow = 2, ncol=2)
)
abpool_sample(estimates, variances, dfcom = 19)

# Example with manually generated model fits after multiple imputation
# Generate data
set.seed(1)
n <- 400; nobs <- 40
X <- rnorm(n); Z <- sqrt(0.9)*rnorm(n) + sqrt(0.1)*X
beta_0 <- 0; beta_X <- 0.8; beta_Z <- 0.2
eta <- beta_0 + X*beta_X + Z*beta_Z
Y <- rbinom(n, size = 1, p = exp(eta)/(1 + exp(eta)) )
X[1:(n-nobs)] <- NA
log.data <- data.frame(Y = Y, X = X, Z = Z)
m <- 200
# Illustrative imputation procedure
estimates <- list(); variances <- list()
for (i in 1:m){
  X[1:(n-nobs)] <- rnorm(n-nobs)
  log.data.imp.i <- data.frame(Y = Y, X = X, Z = Z)
  glm.out.i <- glm(Y ~ X + Z, family = binomial)
  estimates[[i]] <- coef(glm.out.i)
  variances[[i]] <- vcov(glm.out.i)
}
# Sample from ABpool
abpool_sample(estimates,variances, dfcom = Inf)

}
\references{
Phillips, Christodoulou and Steinsaltz (2026)
}
\seealso{
\code{\link[=abpool]{abpool()}} to sample from a \code{mira} object or list of fitted models. \code{\link[=abpool]{abpool()}} also outputs an object which can be analysed by the rest of the package e.g. \code{\link[=qqplot_rubin_abpool]{qqplot_rubin_abpool()}} and \code{\link[=confint.abpool]{confint.abpool()}}, unlike \code{\link[=abpool_sample]{abpool_sample()}}.
}
